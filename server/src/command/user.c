/*
** EPITECH PROJECT, 2020
** server
** File description:
** user.c
*/

#include "user.h"

#include "def/code.h"

int user_command(server_t *server, client_t *client, int argc, char **argv)
{
    (void)(server);
    (void)(client);
    (void)(argc);
    (void)(argv);

    return (CODE_SUCCESS);
}
