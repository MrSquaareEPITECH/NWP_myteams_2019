/*
** EPITECH PROJECT, 2020
** server
** File description:
** subscribe.c
*/

#include "subscribe.h"

#include "def/code.h"

int subscribe_command(server_t *server, client_t *client, int argc, char **argv)
{
    (void)(server);
    (void)(client);
    (void)(argc);
    (void)(argv);

    return (CODE_SUCCESS);
}
