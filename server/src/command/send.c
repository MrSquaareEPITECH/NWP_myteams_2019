/*
** EPITECH PROJECT, 2020
** server
** File description:
** send.c
*/

#include "send.h"

#include "def/code.h"

int send_command(server_t *server, client_t *client, int argc, char **argv)
{
    (void)(server);
    (void)(client);
    (void)(argc);
    (void)(argv);

    return (CODE_SUCCESS);
}
